version: "3.0"

rules:
  #- rule: Show categories depending on user option (TEST)
  #  steps:
  #    - intent: ask_category
  #    - action: action_show_categories

  - rule: Look for articles every time a user makes a query
    steps:
      - intent: choose_category
      - action: action_display_found_articles 

  #- rule: Display the first three articles of a category every time the user wants to
  #  steps:
  #    - intent: choose_category
  #    - action: action_display_category_articles

